# FACOT 2.0 - Variables de Entorno
# Copia este archivo como .env y configura los valores según tu entorno

# ============================================
# FIREBASE / FIRESTORE
# ============================================

# Ruta al archivo JSON de credenciales de Firebase (Service Account)
# Puede usar ruta absoluta o relativa desde el directorio de trabajo
FIREBASE_CREDENTIALS=./firebase-credentials.json

# Bucket de Storage de Firebase
# Formato: {project-id}.firebasestorage.app
FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app

# Alternativa: Variable estándar de Google Cloud
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# ============================================
# BACKUPS
# ============================================

# Directorio donde se guardan los backups
# Por defecto: ./backups
BACKUP_DIR=./backups

# Hora del día para ejecutar backup automático (formato 24h)
# Por defecto: 02:00
BACKUP_HOUR=02:00

# Días de retención de backups (backups más antiguos se eliminan)
# Por defecto: 30
BACKUP_RETENTION_DAYS=30

# ============================================
# NCF (Número de Comprobante Fiscal)
# ============================================

# Longitud del padding numérico en el NCF
# Por defecto: 8 (ej: B01 + 8 dígitos = B0100000001)
NCF_PADDING=8

# Número máximo de reintentos para transacciones de NCF
# Por defecto: 5
MAX_NCF_TX_RETRIES=5

# ============================================
# BASE DE DATOS (Solo para migraciones/backups locales)
# ============================================

# Ruta a la base de datos SQLite local
# Solo se usa para migraciones y backups, NO para runtime
# FACOT_DB_PATH=./data/facturas_db.db

# ============================================
# DESARROLLO / DEBUG
# ============================================

# Usuario para campos de auditoría (created_by, updated_by)
# USER=developer

# Habilitar modo debug
# DEBUG=true
